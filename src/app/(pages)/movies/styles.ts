import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  storybookButton: {
    marginTop: 20,
    marginBottom: 10,
    backgroundColor: "red",
    padding: 10,
    borderRadius: 10,
    alignSelf: "center",
    width: 200,
    textAlign: "center",
  },
  containerFlatList: {
    paddingBottom: 100,
  },
});

export default styles;
