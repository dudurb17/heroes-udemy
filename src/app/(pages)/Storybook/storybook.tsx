import StorybookUIRoot from "../../../../.rnstorybook";

// Expo Router will treat this file as the /storybook route,
// rendering the Storybook UI you configured in `.rnstorybook`.
export default StorybookUIRoot;
